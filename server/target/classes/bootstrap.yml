spring:
  application:
    name: order

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: order
      discovery:
        server-addr: 127.0.0.1:8848
  profiles:
    active: dev
